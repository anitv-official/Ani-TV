# تقرير فحص مشروع AniTV

**تاريخ الفحص:** 23 سبتمبر 2026
**المستودع:** `anitv-official/Ani-TV`، الفرع `main`
**النطاق:** بنية التطبيق، كتالوج المحتوى، البحث، التفاصيل، الحلقات، الأفلام والدراما، المانجا والفصول، التشغيل، التنزيل، الحسابات، المفضلة، السجل، واختبارات المشروع.

## الخلاصة التنفيذية

AniTV هو تطبيق Flutter Android عربي باتجاه RTL. البنية الحالية تفصل بين واجهة المستخدم وبين محولات مصادر المحتوى. وتدعم المحولات عقودًا موحدة للبحث، وأحدث المحتوى، والتفاصيل، وروابط التشغيل، وصور فصول المانجا. هذا التصميم مناسب للتوسع، كما أن `SourceRegistry` يعزل فشل مصدر واحد عن بقية المصادر ويطبق إعادة المحاولة والمهلة والتخزين المؤقت.

الوظائف الأساسية موجودة في الكود، لكنها ليست متساوية في درجة الاكتمال. مسار الأنمي هو الأكثر نضجًا، مع مصادر API وتفاصيل وحلقات وروابط تشغيل. مسار المانجا مكتمل نسبيًا، ويشمل التفاصيل والفصول والصور والقارئ والتنزيل. مسار الدراما والأفلام موجود عبر `DramaSource` وعدة Extensions، لكن توجيه المحتوى يعتمد على شاشة تفاصيل الأنمي نفسها، لذلك يحتاج إلى نموذج محتوى وتسميات وتجربة استخدام أكثر وضوحًا. البحث العام يعمل عبر `SourceRegistry.searchAll`، لكن مسار التصنيفات غير منفذ، وبعض الشاشات ما زالت تحمل آثار طبقة API قديمة.

**الحكم العام:** المشروع قابل للاستمرار، لكنه يحتاج إلى مرحلة توحيد عقود المحتوى والبحث قبل إضافة مصادر جديدة. أخطر نقطة وظيفية حالية هي أن `fetchGenres` و`fetchGenreContent` يعيدان بيانات فارغة دائمًا. وأخطر نقطة بنيوية هي وجود مصدرين للحقيقة في الواجهة: سجل المصادر الجديد من جهة، و`SourceRegistry.all` الفارغ وواجهات `ApiService` القديمة من جهة أخرى. كما أن توثيق الإصدار لا يطابق `pubspec.yaml` وبيانات الإصدار المنشورة.

## خريطة النظام الحالية

يبدأ التطبيق من `lib/main.dart`. تتم تهيئة التنزيلات، وSupabase الخاص بالمجتمع عند توفر الإعداد، ثم Firebase/FCM بصورة غير حاجبة لعرض التطبيق. الحالة العامة للحساب والمفضلة والسجل موجودة في `AppStateProvider`. وتُستخدم Appwrite للمصادقة والملفات الشخصية والمفضلة، بينما يستخدم المجتمع طبقة Supabase/Appwrite حسب الإعداد.

طبقة المحتوى موزعة على المستويات التالية:

| المستوى | المسؤولية | الملفات الرئيسية |
|---|---|---|
| العقد الموحد | تعريف المصدر، البحث، الأحدث، التفاصيل، التشغيل، صور الفصول | `lib/sources/source_base.dart` |
| سجل المصادر | تسجيل المصادر، الدمج، إعادة المحاولة، التخزين المؤقت، توجيه الرابط | `lib/sources/source_registry.dart` |
| مصادر الأنمي | Anime Slayer وAnimefy | `lib/sources/anime_slayer_source.dart`, `lib/sources/animefy_source.dart` |
| مصادر الدراما والأفلام | Drama API وWecima وKormoz | `lib/sources/drama_source.dart`, `lib/sources/web_catalog_source.dart` |
| مصادر المانجا | Manga Swat وMangaTime وMangaMello | `lib/sources/manga_swat_source.dart`, `lib/sources/mangatime_source.dart`, `lib/sources/manga_mello_source.dart` |
| Extensions | Anime3rb وAnime Witcher وEgyDead وKrmzy وAfl aam وAkwam وFaselHD وYouTube | `lib/extensions/providers/` |
| الواجهة | الاستكشاف، البحث، التفاصيل، المشغل، القارئ | `lib/screens/` |
| التخزين والتنزيل | تنزيل الفيديو وصور الفصول، الاستئناف، الحالة المحلية | `lib/services/download_service.dart` |

## تغطية الوظائف

| الوظيفة | الحالة الحالية | الملاحظات |
|---|---|---|
| أحدث الأنمي | تعمل عبر `anime_slayer` في شاشة الاستكشاف والصفحة الرئيسية | `ExploreScreen` يقيد قائمة الأنمي بهذا المصدر في الوضع العام. |
| البحث عن الأنمي | يعمل عبر دمج مصادر الأنمي والدراما | النتيجة تُحوّل إلى تفاصيل الأنمي حتى عندما يكون المصدر دراما. |
| تفاصيل الأنمي | موجودة | تُحمّل التفاصيل وتعرض الحلقات والتصنيفات والتقييم والحالة والمحتوى المرتبط. |
| تشغيل الحلقة | موجود | يعتمد على استخراج روابط مباشرة أو روابط مزود مصادق عليها عبر `SourceRegistry.streams`. |
| الأفلام | موجودة جزئيًا | الفيلم يعالج داخل `AnimeDetailsScreen`، مع استنتاج النوع من الحقول؛ لا توجد شاشة فيلم مستقلة. |
| المسلسلات والدراما | موجودة جزئيًا | `DramaSource` وExtensions مسجلة، لكن النموذج والواجهة يعيدان استخدام مسار الأنمي. |
| أحدث المانجا | تعمل عبر دمج مصادر المانجا | الصفحة الرئيسية تحمل `mangatime` فقط عند التحميل الأول، بينما الاستكشاف يستخدم الدمج. هذا اختلاف غير ضروري. |
| البحث عن المانجا | يعمل عبر `searchManga` | يدعم Manga وManhwa وManhua حسب البيانات الواردة، مع تصنيف واجهة مبسط إلى `comic`. |
| تفاصيل العمل | موجودة | `ComicDetailsScreen` يحاول استنتاج Manga/Manhwa/Manhua من النوع والتصنيفات والرابط والعنوان. |
| قائمة الفصول | موجودة | تشمل البحث والترتيب والتنزيل وفتح القارئ. |
| قارئ الفصول | موجود | يدعم وضع الصفحة ووضع التمرير والتكبير وحفظ الصفحة والتنقل للفصل التالي عند توفر الروابط. |
| تنزيل الفصول | موجود | يحفظ الصور محليًا، ويتطلب تسجيل الدخول. التنزيل الجماعي تسلسلي وليس طابورًا موازيًا. |
| تنزيل الحلقات | موجود | يدعم الطابور والإيقاف والاستئناف والاستكمال الجزئي بملف `.part`. |
| البحث العام | يعمل جزئيًا | يدمج كل المصادر، لكن مصدر YouTube يظهر في «الكل» ولا يدخل في فلاتر الأنمي/المانجا. |
| البحث داخل المصدر | موجود | `SourceContentScreen` يدعم البحث والترقيم، باستثناء مسارات خاصة مثل Drama. |
| التصنيفات والأنواع | غير منفذة | `ApiService.fetchGenres` يعيد `[]` و`fetchGenreContent` يعيد `{'content': []}`. |
| المفضلة | موجودة | مرتبطة بالحساب مع تخزين محلي ومزامنة سحابية. |
| السجل | موجود للحساب | وضع الضيف متعمد أن يكون بلا سجل دائم وفق `AppStateProvider`. |
| المجتمع | موجود كمسار مستقل | لا يدخل في نطاق كتالوج المحتوى، لكنه يزيد تعقيد التهيئة والاعتماديات. |

## النتائج الرئيسية مرتبة حسب التأثير

### 1. التصنيفات لا تعمل فعليًا — أولوية حرجة

في `lib/services/api_service.dart`، الدالتان `fetchGenres` و`fetchGenreContent` تعيدان بيانات فارغة دائمًا. شاشة البحث تستدعيهما في `_loadGenres` و`_performGenreSearch`، ولذلك تظهر واجهة التصنيفات دون محتوى فعلي أو لا تقدم نتائج عند اختيار تصنيف.

**الأثر:** المستخدم لا يستطيع التصفح حسب النوع أو التصنيف، كما أن وجود الكود في الواجهة يوحي بميزة غير مكتملة.

**الإصلاح المقترح:** نقل قدرة التصنيفات إلى عقد `ContentSource` أو إضافة `genres` و`byGenre` اختيارية إلى المصدر. يجب أن يحدد كل مصدر ما إذا كان يدعمها، ثم تدمج النتائج مع نفس سياسة الفشل الجزئي المستخدمة في البحث.

### 2. وجود مصدرين للحقيقة في سجل المصادر — أولوية حرجة

`SourceRegistry._allSources` هو السجل الفعلي، بينما `SourceRegistry.all` ثابت فارغ عمدًا. توجد مراجع مباشرة إلى `SourceRegistry.all` في `profile_screen.dart`، كما أن اختبارات قديمة وخدمات أخرى تعتمد على الاسم نفسه. في المقابل تستخدم شاشة إدارة المصادر `visibleSources`.

**الأثر:** قد تظهر بعض المواضع بلا مصادر رغم أن المصادر مسجلة وتعمل في مواضع أخرى. هذا يسبب سلوكًا غير متسق ويصعب صيانة المشروع.

**الإصلاح المقترح:** إزالة `all` أو تحويله إلى واجهة واضحة تعيد المصادر المرئية حسب الغرض. الأفضل تعريف مجموعات صريحة مثل `catalogSources`, `searchSources`, `videoSources`, و`mangaSources` بدل الاعتماد على قائمة فارغة للتوافق الخلفي.

### 3. صفحة الاستكشاف لا تستخدم نفس سياسة الدمج في كل مكان — أولوية عالية

`ExploreScreen` يجلب الأنمي من `anime_slayer` فقط عند عدم اختيار مصدر، بينما يجلب المانجا من `latestManga`. الصفحة الرئيسية تستخدم `anime_slayer` و`drama_slayer` و`mangatime` وYouTube فقط في التحميل الأول. هذا يختلف عن قدرات `SourceRegistry` التي تستطيع دمج المصادر حسب النوع.

**الأثر:** نتائج الأنمي والمانجا تختلف باختلاف مكان الدخول. وقد لا يرى المستخدم محتوى مصدر مسجل ومرئي في الصفحة الرئيسية.

**الإصلاح المقترح:** اعتماد دوال موحدة مثل `latestByCategory(ContentCategory category, page)` في السجل، واستخدامها في الصفحة الرئيسية والاستكشاف والبحث. يظل فتح مصدر محدد مسارًا منفصلًا وواضحًا.

### 4. الأفلام والدراما تستخدم نموذج شاشة الأنمي — أولوية عالية

يتم توجيه عناصر الفئة `drama` و`movie` إلى `AnimeDetailsScreen`. الشاشة تستنتج الفيلم من النوع أو الفئة، ثم تعرض زرًا ومعلومات بصياغة أنمي في أجزاء من المسار. هذا يسمح بالتشغيل في بعض الحالات، لكنه لا يمثل نموذجًا واضحًا للمواسم أو الحلقات أو الأفلام.

**الأثر:** التجربة مربكة، وتزداد احتمالات أخطاء الحقول؛ فمصدر الدراما قد يعيد مواسم وحلقات مختلفة عن بنية `episodes` المتوقعة في الأنمي.

**الإصلاح المقترح:** تعريف `ContentType` موحد، ثم إنشاء `VideoDetailsScreen` عامة أو فصل `AnimeDetailsScreen` و`DramaDetailsScreen` مع مكونات مشتركة. يجب تحويل كل مصدر إلى نموذج داخلي typed بدل تمرير `Map<String, dynamic>` عبر كل الطبقات.

### 5. الدمج يزيل التكرار بمفتاح ضعيف — أولوية عالية

`SourceRegistry._merge` يستخدم المفتاح `url|source_id|title`. اختلاف بسيط في العنوان أو الرابط بين مصدرين يمنع إزالة التكرار، بينما العمل نفسه قد يظهر عدة مرات. وفي المقابل لا يوجد تطبيع موحد للعنوان العربي والإنجليزي أو رقم الموسم.

**الأثر:** نتائج البحث والصفحة الرئيسية قد تحتوي على نسخ مكررة من نفس العمل، ولا يوجد تجميع للعمل مع مصادره وروابطه.

**الإصلاح المقترح:** إضافة `ContentIdentity` يعتمد على معرف المصدر عند الحاجة، وعلى عنوان مطبع مع سنة ونوع عند الدمج بين المصادر. يجب الاحتفاظ بقائمة المصادر البديلة بدل حذفها أو عرضها كعناصر متكررة.

### 6. الاختبار المحلي غير قابل للتنفيذ في البيئة الحالية — أولوية عالية للتسليم

تم فحص المشروع، لكن Flutter غير مثبت في بيئة التنفيذ؛ لذلك تعذر تشغيل `flutter pub get` و`flutter analyze` و`flutter test`. توجد 11 ملفات اختبار، تشمل مصادر الدراما وExtensions وMangaTime والتخزين المحلي والمجتمع، لكنها لا تغطي بشكل كافٍ تدفق البحث العام، التصنيفات، تفاصيل الأفلام، التشغيل، والتنزيلات.

**الإصلاح المقترح:** تشغيل CI الموجود في `.github/workflows/android.yml` على كل تغيير، وإضافة اختبارات وحدات لعقد `SourceRegistry`، واختبارات widget لشاشات البحث والتفاصيل، واختبارات تكامل mock لاستجابات كل نوع محتوى. يجب توثيق نتيجة آخر CI داخل كل إصدار بدل الاعتماد على تقرير قديم.

### 7. توثيق الإصدار متأخر عن الكود — أولوية متوسطة

`pubspec.yaml` يعلن `1.3.2+12`، بينما README يعلن `1.2.1+2`، و`website/release.json` يشير إلى `v1.3.1`. كذلك README يربط مستودعًا قديمًا هو `lo-oord/Ani-TV`، في حين أن المستودع الحالي هو `anitv-official/Ani-TV`.

**الأثر:** المستخدم أو CI قد يحمّل إصدارًا غير مطابق، وتصبح صفحة About وروابط التحديث أقل موثوقية.

**الإصلاح المقترح:** جعل رقم الإصدار مصدرًا واحدًا. عند إنشاء release يجب تحديث `pubspec.yaml` و`website/release.json` وREADME تلقائيًا أو إزالة الأرقام الثابتة من README، وتوحيد رابط المستودع الحالي.

### 8. عقد النتائج يعتمد على خرائط غير typed — أولوية متوسطة

المصادر والشاشات تتبادل مفاتيح نصية مثل `type`, `category`, `episodes`, `chapters`, `stream_url`, و`direct_stream_urls`. توجد تحويلات مباشرة مثل `as List<dynamic>?` و`as Map<String, dynamic>` في الشاشات.

**الأثر:** استجابة مصدر واحدة بحقل مختلف قد تسبب شاشة فارغة أو استثناء وقت التشغيل بدل رسالة مصدر مفهومة.

**الإصلاح المقترح:** إضافة نماذج `ContentItem`, `ContentDetails`, `Episode`, `Chapter`, و`StreamOption`. يتم تحويل استجابة كل مصدر مرة واحدة داخل محوله، وتتعامل الواجهة مع نماذج ثابتة فقط.

### 9. التنزيل الجماعي للفصول بطيء وغير قابل للاستئناف على مستوى الفصل — أولوية متوسطة

`ComicDetailsScreen._downloadAllChapters` ينفذ تنزيل الفصول واحدًا تلو الآخر، ويعيد فحص كل فصل عند إعادة العملية. خدمة الفيديو تملك طابورًا بحد أقصى لتنزيلين متزامنين، لكن صور الفصول لا تستخدم نفس الطابور.

**الإصلاح المقترح:** تحويل تنزيل الفصل إلى مهمة ذات حالة دائمة، مع طابور مشترك، واستئناف لكل صورة، وإظهار عدد المهام المتبقية بدل انتظار العملية كاملة داخل الشاشة.

### 10. التعامل مع الروابط الخارجية يحتاج عقدًا أوضح — أولوية متوسطة

`SourceRegistry.streams` يمنع كثيرًا من روابط صفحات المشغل، وهذا قرار أمني وتجريبي جيد. لكنه يخلط بين الروابط المباشرة وروابط embed المسموح بها حسب المصدر، وتظهر في الكود قوائم نطاقات ثابتة كثيرة.

**الإصلاح المقترح:** تعريف `StreamOption` يحوي `kind` و`provider` و`headers` و`requiresWebView`. يقرر المصدر نوع الرابط، بينما تطبق طبقة التشغيل سياسة موحدة تمنع النوافذ المنبثقة والتحويلات غير المسموح بها.

## نقاط قوة مهمة

يملك `SourceRegistry` آلية جيدة لإعادة المحاولة بمهلة 35 ثانية، مع تنفيذ متوازٍ واحتواء فشل كل مصدر عند دمج النتائج. كما يمنع تمرير بعض صفحات المشغلات الخارجية إلى المشغل الأصلي عندما لا تكون روابط وسائط صالحة.

خدمة التنزيل تحفظ حالة المهمة محليًا، وتتعامل مع الإيقاف والاستئناف والإلغاء، وتستخدم ملفات مؤقتة لتقليل تلف الملفات عند الانقطاع. كما تتحقق من الملفات المكتملة بعد إعادة تشغيل التطبيق.

توجد معالجة جيدة نسبيًا للتزامن في `AppStateProvider`؛ إذ تعيد `initialize()` نفس العملية الجارية عند الاستدعاءات المتزامنة، وتفصل بيانات المستخدم عبر `userId`. كما أن الاختبارات الحالية تتحقق من عدم تخزين كلمات المرور أو الأسرار في التخزين المحلي.

القارئ يدعم حفظ موضع الصفحة، والتكبير، ووضع الصفحة أو التمرير، والتنقل التلقائي للفصل التالي عندما يعيد المصدر روابط التنقل. هذه وظائف مناسبة لتجربة قراءة فعلية وليست مجرد عرض صور.

## خطة العمل المقترحة

### المرحلة الأولى: تثبيت العقود

ينبغي أولًا تعريف نماذج المحتوى الداخلية وإزالة الخرائط العامة من واجهات الشاشات. في الوقت نفسه يجب توحيد سجل المصادر وإلغاء `SourceRegistry.all` الفارغ أو إعادة تسميته بوضوح. هذه المرحلة تقلل أخطاء التشغيل قبل أي توسع.

### المرحلة الثانية: توحيد الكتالوج والبحث

يجب أن تستخدم الصفحة الرئيسية والاستكشاف والبحث دوالًا موحدة بحسب الفئة. بعد ذلك تُنفذ التصنيفات لكل مصدر يدعمها، مع حالة واضحة للمصادر التي لا تدعم التصنيف. يجب إضافة إزالة تكرار تعتمد على هوية العمل وتجميع المصادر البديلة.

### المرحلة الثالثة: فصل الفيديو عن الأنمي

ينبغي فصل نموذج الفيديو العام عن نموذج الأنمي. يدعم النموذج العام فيلمًا، مسلسلًا، موسمًا، وحلقة، بينما يبقى الأنمي نوعًا متخصصًا عند الحاجة. بهذه الطريقة تظهر الدراما والأفلام بعناوين صحيحة دون كسر المشغل الحالي.

### المرحلة الرابعة: الاختبارات والإصدار

يجب تثبيت Flutter في بيئة CI محددة الإصدار، وتشغيل التحليل والاختبارات والبناء قبل الدمج. ثم تضاف اختبارات mock لكل عقد مصدر، واختبارات لتصنيف نتائج البحث، واختبارات لتنزيل فصل وفيديو مع انقطاع واستئناف. أخيرًا يوحّد الإصدار في ملف واحد وتُحدّث بيانات الموقع آليًا.

## قائمة تحقق قبول الإصدار القادم

- يعرض البحث العام نتائج الأنمي والدراما والأفلام والمانجا والفيديو مع فلاتر صحيحة.
- تعمل التصنيفات أو تختفي من الواجهة عندما لا يدعمها أي مصدر.
- تستخدم الصفحة الرئيسية والاستكشاف نفس سياسة مصادر الكتالوج.
- تفتح الأفلام والدراما في شاشة ومعلومات متوافقة مع المواسم والحلقات.
- لا تظهر العناصر المكررة للعمل نفسه إلا إذا كانت مصادر بديلة مقصودة.
- يستطيع المستخدم فتح فصل محفوظ وقراءة الصور دون اتصال.
- يستطيع المستخدم إيقاف تنزيل فيديو أو فصل واستئنافه بعد إغلاق التطبيق.
- تمر أوامر `flutter analyze`, `flutter test`, و`flutter build apk --release` في CI.
- يتطابق رقم الإصدار بين `pubspec.yaml` وبيانات الموقع وGitHub Release.
- لا توجد أسرار إدارية أو مفاتيح خدمة داخل APK أو سجلات التطبيق.

## نتيجة الفحص

المشروع ليس هيكلًا فارغًا؛ فهو يحتوي على نظام محتوى حقيقي ومصادر متعددة وتجربة تشغيل وقراءة وتنزيل وحسابات. المشكلة الأساسية ليست غياب الوظائف، بل **عدم توحيدها**. الأولوية الصحيحة هي تثبيت عقد المحتوى، وإصلاح التصنيفات، وتوحيد مصادر الكتالوج والبحث، ثم فصل الدراما والأفلام عن واجهة الأنمي. بعد ذلك يصبح إضافة مصدر جديد أقل مخاطرة وأقل كلفة صيانة.

## References

[1]: https://github.com/anitv-official/Ani-TV "AniTV GitHub repository"
[2]: https://docs.flutter.dev/testing "Flutter testing documentation"
[3]: https://docs.flutter.dev/deployment/android "Flutter Android deployment documentation"
