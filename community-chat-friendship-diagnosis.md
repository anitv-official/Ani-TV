# تشخيص مشاكل الشات وطلبات الصداقة في Ani-TV

**تاريخ الفحص:** 22 سبتمبر 2026  
**المستودع:** `anitv-official/Ani-TV`  
**الفرع:** `main`  
**Commit المحلي المفحوص:** `3c8676f`  
**مشروع Supabase:** `AniTV` (`wmzeydetzfndkpgqwfjd`)

## الخلاصة التنفيذية

المشكلة الأساسية في الشات مؤكدة وليست في إنشاء المحادثة أو عضويتها. التطبيق ينشئ المحادثة ويضيف العضوين عبر Edge Function باستخدام هوية Appwrite الموثوقة، لكنه يحاول لاحقًا تحميل قائمة المحادثات والرسائل عبر Supabase REST مباشرةً باستخدام مفتاح العميل. سياسات RLS تعتمد على `community_current_user_id()`، وهذه الدالة لا ترى هوية Appwrite داخل طلب Flutter المباشر؛ لذلك تفشل القراءة برسالة `permission denied` وHTTP 401.

أما طلبات الصداقة، فالفحص الإنتاجي أثبت أنها تصل إلى قاعدة البيانات وتُنشأ لها إشعارات. المشكلة الحالية في هذا المسار هي عدم تطابق النسخة المنشورة من `community-write` مع الكود المحلي، إضافة إلى أخطاء في إدارة الحالات والعرض قد تجعل الطلب يبدو وكأنه لم يصل.

## الأدلة المؤكدة من الإنتاج

| الفحص | النتيجة |
|---|---|
| وظائف Supabase | `community-write` منشورة بإصدار 9 وحالتها ACTIVE |
| الحسابات | يوجد حسابان في `community_profiles` |
| طلبات الصداقة | سجلان موجودان: واحد `accepted` وواحد `pending` |
| إشعارات طلبات الصداقة | سجلان موجودان ومرتبطان بـ `friend_request_id` |
| المحادثات | محادثة واحدة موجودة |
| أعضاء المحادثة | عضوان موجودان في المحادثة |
| الرسائل | لا توجد رسائل حتى وقت الفحص |

السجلات الإنتاجية أظهرت تكرارًا واضحًا للخطأ التالي أثناء فتح الشات:

```text
permission denied for function community_current_user_id
GET .../rest/v1/community_conversation_members?... 401
```

وفي المقابل، أظهرت السجلات أن استدعاءات Edge Function الخاصة بقراءة الإشعارات وطلبات الصداقة تعيد HTTP 200، وأن طلب الصداقة نفسه أدى إلى إدخال ناجح في قاعدة البيانات.

## السبب الأول والرئيسي: قراءة الشات مباشرةً مع RLS وهوية غير صحيحة

في `lib/community/repositories/supabase_community_repositories.dart`:

- `conversations()` في الأسطر 461–475 يقرأ `community_conversation_members` مباشرةً.
- `messages()` في الأسطر 490–499 يقرأ أعضاء المحادثة والرسائل مباشرةً.
- `_assertMember()` في الأسطر 542–551 يتحقق من العضوية عبر Supabase REST المباشر.

هذه الطلبات لا تمر عبر `CommunityWriteApi` ولا ترسل `x-appwrite-jwt`. لذلك لا يمكن للدالة `community_current_user_id()` استخراج هوية المستخدم. النتيجة العملية:

1. شاشة الرسائل تظهر فارغة أو تفشل في التحميل.
2. شاشة المحادثة تفشل قبل عرض الرسائل بسبب `_assertMember()`.
3. إرسال الرسالة لا يصل إلى مرحلة الإدخال لأن تحميل المحادثة يفشل أولًا.
4. المحادثة الموجودة في قاعدة البيانات لا تعني أن التطبيق يستطيع قراءتها من العميل.

### الإصلاح المطلوب

يجب توحيد المسار الحساس للمحادثات عبر Edge Function، بإضافة Actions مثل:

- `list_conversations`
- `list_messages`
- ويمكن لاحقًا `mark_messages_read`

أو إنشاء عميل قراءة موثوق يرسل JWT Appwrite ويُترجم إلى هوية Supabase، لكن الحل المتوافق مع التصميم الحالي هو تمرير كل قراءات المحادثات الخاصة عبر `community-write` أو Edge Function قراءة مخصصة.

لا ينبغي تخفيف RLS أو السماح بقراءة المحادثات اعتمادًا على `user_id` القادم من Flutter.

## السبب الثاني: النسخة المنشورة من Edge Function ليست مطابقة للكود المحلي

النسخة المنشورة `community-write` الإصدار 9 تختلف عن الملف المحلي `supabase/functions/community-write/index.ts`.

### فروق مؤثرة

1. **قبول طلب الصداقة في النسخة المنشورة لا ينشئ إشعار قبول**، بينما الكود المحلي يفعل ذلك في السطر 96:

```ts
await supabaseInsert(config, "community_notifications", {
  recipient_id: request.requester_id,
  actor_id: identity.id,
  type: "friend_request_accepted",
  friend_request_id: requestId
});
```

2. **النسخة المنشورة لا تحدّث `updated_at` للمحادثة عند إرسال رسالة**، بينما الكود المحلي يفعل ذلك في السطر 168. لذلك قد لا تظهر المحادثة في ترتيبها الصحيح.

3. **النسخة المنشورة لا تحتوي على كل تقويات التحقق الموجودة محليًا**، مثل منع إرسال طلب صداقة للنفس والتعامل مع الطلب الموجود سابقًا.

4. **النسخة المنشورة لا تحتوي على كل إصلاحات الحذف والتحقق الموجودة في الملف المحلي الحالي.**

### الإصلاح المطلوب

إعادة نشر `community-write` من نفس Commit المحلي بعد التأكد من متغيرات البيئة، ثم تنفيذ Smoke Test لكل Action. لا يكفي تعديل الملف في GitHub؛ النسخة المنشورة يجب أن تتحدث فعليًا.

## السبب الثالث في طلبات الصداقة: الطلبات تصل، لكن دورة الحالة غير مكتملة

قاعدة البيانات تؤكد وجود:

- طلب `pending` للمستلم.
- إشعار `friend_request` للمستلم.
- طلب `accepted` سابقًا، مع بقاء إشعاره القديم من النوع `friend_request`.

هذا يعني أن عبارة «الطلب لا يصل» ليست مشكلة إدخال أساسية في الحالة المفحوصة؛ بل توجد مشاكل في العرض ودورة الحالة:

1. بعد القبول لا يتم تعليم إشعار الطلب كمقروء أو حذفه.
2. الإشعار القديم يبقى من النوع `friend_request` حتى بعد أن أصبح الطلب `accepted`.
3. النسخة المنشورة لا ترسل إشعار `friend_request_accepted`.
4. `NotificationType` في Flutter لا يحتوي نوعًا مستقلًا لـ `friend_request_accepted`؛ أي قيمة غير `friend_request` أو `comment` تتحول إلى `reaction` في `_notificationType()`.
5. عند فشل `friend_status` داخل `getProfile()` يتم ابتلاع الخطأ وإرجاع الحالة الافتراضية `none`، ما قد يعرض زر إضافة صديق رغم وجود طلب سابق.

### الإصلاح المطلوب

- إضافة `friendRequestAccepted` إلى نموذج الإشعارات.
- فصل الإشعار المقروء عن حالة طلب الصداقة.
- إضافة Action لتعليم الإشعار كمقروء.
- بعد القبول، تحديث الطلب والإشعار والحالة في الواجهة بشكل صريح.
- عدم ابتلاع خطأ `friend_status` دون تسجيله؛ يجب إظهاره أو تسجيله حتى لا يتحول إلى حالة `none` مضللة.
- جعل `send_friend_request` يتعامل بأمان مع الطلب النشط الموجود بدل الاعتماد على خطأ unique constraint.

## السبب الرابع: عدم ضمان وجود Community Profile للمستلم

`ensureProfile()` ينشئ ملف Community للمستخدم الحالي فقط. عند إرسال طلب أو فتح محادثة مع مستخدم لم تتم مزامنته بعد، قد يفشل الإدخال بسبب Foreign Key على:

- `community_friend_requests.recipient_id`
- `community_conversation_members.user_id`

### الإصلاح المطلوب

قبل إنشاء طلب أو عضو محادثة، يجب التأكد من وجود ملف Community للمستخدم المستهدف أو تنفيذ مزامنة موثوقة له من Appwrite. لا يجب إنشاء سجل Profile اعتمادًا على بيانات يرسلها العميل؛ يجب استخدام مصدر موثوق أو مسار مزامنة داخلي.

## أخطاء واجهة الشات الأخرى

### 1. قائمة المحادثات تفقد هوية الطرف الآخر

في `conversations()` يتم إنشاء `Conversation` باستخدام:

```dart
participant: const PostAuthor(
  id: '',
  username: 'community',
  displayName: 'Community',
)
```

لذلك كل محادثة تظهر باسم عام، ومعرّف الطرف الآخر فارغ. الضغط على عنوان المحادثة يفتح ملف مستخدم بمعرّف فارغ.

**الإصلاح:** جلب أعضاء المحادثة الآخرين وملفاتهم وإرجاع `participant` الحقيقي.

### 2. الرسائل لا تتحدث لحظيًا

`ConversationScreen` يحمل الرسائل مرة واحدة في `initState()` فقط. لا يوجد Realtime listener مربوط بالشاشة، رغم وجود عقد منفصل `CommunityRealtimeRepository` في المشروع.

**الأثر:** رسالة الطرف الآخر لن تظهر إلا بعد إعادة فتح الشاشة أو إعادة تحميلها.

**الإصلاح:** إضافة Supabase Realtime عبر طبقة موثوقة أو Edge-backed polling مناسب، مع منع تكرار الرسائل عند وصول الرسالة محليًا ثم من Realtime.

### 3. تحديد رسائل المستخدم الحالي خاطئ

في `_MessageBubble`:

```dart
final mine = message.senderId == 'guest';
```

المستخدم الحقيقي معرّفه Appwrite مثل `6aa9a4a4d28b0cce4a0c`، وليس `guest`. لذلك رسائل المستخدم الحالي تظهر بمحاذاة الطرف الآخر، ولا تظهر حالة القراءة بالشكل الصحيح.

**الإصلاح:** تمرير `currentUserId` إلى الشاشة والمكوّن، ومقارنة `message.senderId` به.

### 4. لا توجد معالجة كافية لأخطاء الإرسال

`_send()` يرفع الصورة ثم يرسل الرسالة دون حالة إرسال أو `try/catch` واضحة أو Retry. فشل رفع الصورة أو إدخال الرسالة قد يترك الواجهة في حالة غير مفهومة.

**الإصلاح:** إضافة حالة `sending`, رسالة خطأ مفهومة، إعادة محاولة، ومنع الضغط المكرر.

## ملاحظات على النشر والاختبار

- فحص `git diff --check` لم يظهر أخطاء تنسيق.
- أدوات `dart` و`deno` غير مثبتة في بيئة الفحص الحالية؛ لذلك لم يمكن تشغيل `dart analyze` أو `deno check` محليًا.
- GitHub Actions هو المرجع المناسب للتحليل والبناء.
- يجب عدم تنفيذ تغييرات مباشرة في الإنتاج قبل إصلاح المسارات واختبارها بحسابين حقيقيين.

## ترتيب الإصلاح المقترح

1. إصلاح قراءات المحادثات الخاصة: `list_conversations` و`list_messages` عبر Edge Function.
2. إعادة نشر `community-write` من الكود المحلي الحالي ومطابقة الإصدار المنشور.
3. إصلاح `participant` الحقيقي في قائمة المحادثات.
4. إصلاح مقارنة `senderId` بدل القيمة الثابتة `guest`.
5. إضافة نوع إشعار `friend_request_accepted` وتعليم الإشعارات كمقروءة.
6. ضمان مزامنة Profile للمستلم قبل إنشاء الصداقة أو المحادثة.
7. إضافة Realtime للمحادثات واختبارات منع التكرار.
8. تنفيذ اختبار حسابين: طلب صداقة، قبول، فتح محادثة، إرسال نص، إرسال صورة، وظهور المحتوى لدى الطرفين.
9. تشغيل `flutter analyze` وFlutter tests وAndroid Build وFunction Quality قبل أي Commit نهائي.

## الحكم النهائي

- **الشات لا يعمل أساسًا بسبب تصميم قراءة خاطئ:** عمليات القراءة الخاصة تمر مباشرة من Flutter إلى Supabase RLS دون هوية Appwrite، والدليل سجلات HTTP 401 ورسالة `permission denied for function community_current_user_id`.
- **طلبات الصداقة تصل إلى قاعدة البيانات في الحالة المفحوصة:** يوجد طلب Pending وإشعار مرتبط به. المشكلة المتبقية في هذا المسار هي عدم تطابق Edge Function المنشورة، وعدم اكتمال إدارة حالة الإشعار، وإخفاء بعض أخطاء الهوية/الحالة في Flutter.
- **لا توجد رسائل في المحادثة الإنتاجية المفحوصة:** وهذا متوقع لأن شاشة المحادثة تفشل في القراءة قبل أن يستطيع المستخدم إرسال أول رسالة.

هذه النتائج تبرر البدء بإصلاح الشات ونسخة Edge Function قبل إضافة ميزات جديدة أو تعديل تصميم الواجهة.
