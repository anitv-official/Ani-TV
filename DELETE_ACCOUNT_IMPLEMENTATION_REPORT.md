# تقرير تنفيذ ميزة حذف الحساب في AniTV

## الحالة

تمت إضافة واجهة حذف الحساب وتدفق التحقق والتأكيد والتنظيف المحلي، مع تنفيذ الحذف السحابي داخل الـFunction الحالية بعد إضافة مسار محمي `delete_account`. لا يستخدم Flutter API Secret ولا يستدعي حذف الحساب من Client SDK.

## الملفات المعدلة

| المسار | التعديل |
|---|---|
| `lib/screens/profile_screen.dart` | إضافة خيار حذف الحساب، تحذير نهائي، مربعَي موافقة، تأكيد كلمة المرور مرتين، وتأكيد نهائي ورسائل خطأ واضحة. |
| `lib/providers/app_state_provider.dart` | إضافة نقطة دخول لحذف الحساب وتنظيف حالة المستخدم المحلية بعد نجاح عملية الحذف. |
| `lib/services/appwrite_service.dart` | استدعاء مسار الحذف في Function الحالية عبر تنفيذ متزامن، دون تخزين كلمة المرور أو تسجيلها أو وضع Secret في التطبيق. |
| `lib/services/local_cache_service.dart` | إضافة تنظيف انتقائي لمفاتيح الحساب المحلية مع عدم حذف التنزيلات أو إعدادات التطبيق العامة. |
| `appwrite-functions/username-login/src/main.js` | إضافة مسار `delete_account` داخل الـFunction الوحيدة الحالية، مع التحقق من هوية الجلسة وملكية الموارد وحذفها عبر Server SDK. |
| `appwrite-functions/username-login/README.md` | توثيق متغيرات البيئة والصلاحيات المطلوبة وبروتوكول الحذف. |

## البيانات التي فحصها المشروع

يستخدم التطبيق Appwrite Account، وTablesDB لجدول Profiles، وDatabases لجدول Favorites، وStorage لصور الملف الشخصي. لا توجد History سحابية في الكود الحالي؛ History محلية عبر SharedPreferences. التنزيلات ملفات محلية ولا يتم حذفها ضمن هذا التغيير.

## الحماية

التدفق يعتمد على المستخدم الحالي و`userId`. عمليات Favorites الحالية تتحقق من المستخدم الحالي قبل القراءة أو التعديل. لم تتم إضافة أي عملية حذف تعتمد على `documentId` وحده، ولم تتم إضافة أي Secret أو API Key إلى تطبيق Flutter.

## Google/OAuth

الحسابات التي لا تملك كلمة مرور محلية لا يمكن إعادة التحقق منها عبر تدفق كلمة المرور الحالي. لم يتم تغيير Google Login، ويظهر للمستخدم أن الحذف يحتاج إلى مسار خادمي آمن بدل افتراض كلمة مرور أو حذف الحساب دون تحقق.

## الإعداد المطلوب لإكمال الحذف الفعلي

يلزم تحديث الـFunction الحالية ونشر الإصدار الجديد، وإضافة `APPWRITE_FAVORITES_TABLE_ID` و`APPWRITE_PROFILE_IMAGES_BUCKET_ID` ومنح API Key الخاص بها صلاحيات الحذف المطلوبة. لا يجوز وضع API Secret داخل تطبيق Flutter.

## الاختبارات

تم تشغيل `node --check` للـFunction و`git diff --check` ومراجعة الاستدعاءات الجديدة وفحص عمليات الحذف يدويًا، ولم تظهر أخطاء JavaScript أو عمليات حذف غير محمية. تعذر تشغيل `dart format` و`flutter analyze` و`flutter test` لأن الأمرين `dart` و`flutter` غير موجودين في بيئة التنفيذ. يجب تشغيلها في بيئة Flutter قبل الدمج.
